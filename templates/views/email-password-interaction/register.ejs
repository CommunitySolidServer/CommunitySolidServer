<!DOCTYPE html>
<html >
  <head>
    <meta charset="UTF-8">
    <title>Register</title>
    <style type="text/css">
      <%- include('main.css', {}); %>
     </style>
  </head>
  <body>
    <div class="main-content">
      <section class="main-content-section">
        <div class="wrapper">
          <h1 class="title--white">Register</h1>
          <div class="login-panel">
            <div class="panel-body">
              <form autocomplete="off" action="<%= details.pathPrefix %>/idp/interaction/<%= details.uid %>/register" method="post">

                <%if (errorMessage) { %>
                  <div class="input-wrap">
                    <label style="color: #D0021B">
                      <%= errorMessage %>
                    </label>
                  </div>
                <% } %>

                <div class="input-wrap">
                  <label>Email:</label>
                  <input type="text" name="email" autofocus="on" <% if (prefilled.email) { %> value="<%= prefilled.email %>" <% } %> />
                </div>

                <div class="input-wrap">
                  <label>WebId:</label>
                  <input type="text" name="webId" <% if (prefilled.webId) { %> value="<%= prefilled.webId %>" <% } %> />
                </div>

                <div class="input-wrap">
                  <label>Password:</label>
                  <input type="password" name="password" />
                </div>

                <div class="input-wrap">
                  <label>Confirm Password:</label>
                  <input type="password" name="confirmPassword" />
                </div>

                <div class="input-wrap">
                  <label class="checkbox"><input type="checkbox" name="remember" value="yes" checked="yes">Stay signed in</label>
                </div>

                <button type="submit" name="submit" class="ids-link-filled">Create Identity</button>

                <hr />
                <div class="space-between">
                  <a href="<%= details.pathPrefix %>/idp/interaction/<%= details.uid %>/login" class="link">Sign In</a>
                </div>
              </form>
            </div>
          </div>
        </div>
      </section>
    </div>
  </body>
</html>